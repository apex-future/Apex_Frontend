# Production (Vercel): leave unset — the app uses /api/admin and Vercel proxies to the backend.
# Local dev with backend on localhost:
# VITE_API_BASE_URL=http://localhost:8000/api/admin
# Local dev with Vite proxy (default): leave unset and use npm run dev
